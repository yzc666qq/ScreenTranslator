<Window x:Class="ScreenTranslator.App.Windows.TranslationOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        AllowsTransparency="True"
        Background="Transparent"
        Focusable="False"
        ResizeMode="NoResize"
        ShowActivated="False"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStyle="None">
    <Border x:Name="OverlaySurface"
            Padding="12"
            Background="#E60D1218"
            BorderBrush="#5932414D"
            BorderThickness="1"
            CornerRadius="10">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto">
            <TextBlock x:Name="TranslationText"
                       FontFamily="Cascadia Mono, Consolas, Microsoft YaHei UI"
                       FontSize="18"
                       Foreground="#FFF3F7FA"
                       LineHeight="26"
                       TextWrapping="Wrap" />
        </ScrollViewer>
    </Border>
</Window>
